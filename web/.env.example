# Public API base URL used by the browser.
# For same-origin production deployments, use /api/v1.
VITE_API_URL=http://localhost:8000/api/v1

# Enable the Vue DevTools Vite plugin only when needed.
VITE_ENABLE_VUE_DEVTOOLS=false
